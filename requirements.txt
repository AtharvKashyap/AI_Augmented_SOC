# AI_Augmented_SOC Python dependencies
#
# Keep this file small during the MVP. Add dependencies only when they are
# actually used by the codebase.

# Environment/config loading
python-dotenv>=1.2.2

# HTTP clients for Wazuh, Security Onion, OpenRouter, and enrichment APIs
requests>=2.34.2

# Testing
pytest>=9.0.3

# Linting / code quality.
# Pinned to a minor range on purpose: the enabled rule set changes between
# Ruff minor releases, so an unpinned linter can fail CI on a release that has
# nothing to do with the change under test. See ruff.toml for the rule set.
ruff>=0.16.6,<0.17